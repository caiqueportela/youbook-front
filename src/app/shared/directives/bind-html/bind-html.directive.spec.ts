import { BindHtmlDirective } from './bind-html.directive';

describe('BindHtmlDirective', () => {
  it('should create an instance', () => {
    const directive = new BindHtmlDirective();
    expect(directive).toBeTruthy();
  });
});
