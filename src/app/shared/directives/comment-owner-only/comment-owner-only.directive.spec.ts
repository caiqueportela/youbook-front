import { CommentOwnerOnlyDirective } from './comment-owner-only.directive';

describe('CommentOwnerOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new CommentOwnerOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
