import { ShowIfAuthorDirective } from './show-if-author.directive';

describe('ShowIfAuthorDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowIfAuthorDirective();
    expect(directive).toBeTruthy();
  });
});
