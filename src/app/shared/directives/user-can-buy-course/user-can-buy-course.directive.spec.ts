import { UserCanBuyCourseDirective } from './user-can-buy-course.directive';

describe('UserCanBuyCourseDirective', () => {
  it('should create an instance', () => {
    const directive = new UserCanBuyCourseDirective();
    expect(directive).toBeTruthy();
  });
});
