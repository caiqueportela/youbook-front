import { PostOwnerOnlyDirective } from './post-owner-only.directive';

describe('PostOwnerOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new PostOwnerOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
