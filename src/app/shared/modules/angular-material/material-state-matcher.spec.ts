import { MaterialStateMatcher } from './material-state-matcher';

describe('MaterialStateMatcher', () => {
  it('should create an instance', () => {
    expect(new MaterialStateMatcher()).toBeTruthy();
  });
});
